name = "demo"
type = "plugin.consumer"
version = "0.1.0"

requires = [
  "telemetry.car.v1",
  "telemetry.session.v1"
]

[logic]
module = "plugins.demo.plugin"
class  = "DemoPlugin"

[widgets]
file = "widgets/widgets.toml"

# TODO(0.3.x): editors become relevant later as the user-facing place for
# compounds, vehicle-specific data, and other removable or editable records.
[editors]
file = "editors/editors.toml"
