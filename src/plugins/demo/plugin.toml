name = "demo"
type = "plugin.consumer"
version = "0.1.0"

requires = [
  "telemetry.car.v1",
  "telemetry.session.v1"
]

[logic]
module = "plugins.demo.plugin"
class  = "DemoPlugin"

[ui]
widgets = "widgets.toml"

[editors]
file = "editors.toml"
