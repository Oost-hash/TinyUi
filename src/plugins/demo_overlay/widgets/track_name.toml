[widget]
id = "track_name"
widget = "textWidget"
title = "Track"
description = "Current circuit name from the active connector source."
label = "Track"

[bindings]
source = "track.name"

[defaults]
enabled = true
position = [100, 180]
